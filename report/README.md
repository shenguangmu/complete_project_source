# 设计报告

> 本目录是赛题 §3.3.5.3「设计报告」的存放位置。

## 索引

| 文件 | 内容 | 状态 |
|---|---|---|
| [`design.md`](design.md) | **设计报告正文**（七节） | ✅ 除 §4.4 外完成 |
| [`llm-collaboration.md`](llm-collaboration.md) | **大模型协作记录**（含自我纠错轨迹） | ✅ 完成 |
| [`../src_hls/optimization-report.md`](../src_hls/optimization-report.md) | **优化对照实验 + 时序分析**（§4 的详细数据） | ✅ 完成 |

**不在本目录、但同属提交物的**：

| 内容 | 位置 |
|---|---|
| 技能包（赛题 §3.3.5.2） | [`../skill/README.md`](../skill/README.md) |
| 综合与实现报告（§3.3.5.1） | [`../vivado/build-report.md`](../vivado/build-report.md) |
| 复现说明 | 根 [`README.md`](../README.md) 的「从零构建完整工程」 |
| 目录对照说明（§3.3.5.4） | [`../docs/README.md`](../docs/README.md) |

---

## 各节状态

| 节 | 内容 | 状态 |
|---|---|---|
| ① 选题背景与创新点 | 三个创新点 + 前人工作（**含 3 篇核验过 DOI 的文献**） | ✅ |
| ② 设计原理与功能框图 | 系统总览、两条数据通路、时钟域 | ✅ |
| ③ 软硬件划分与接口 | 划分判据、唯一接口契约、96×96 重排理由 | ✅ |
| ④ 优化过程 | 实测数据 + **一次失败的优化（有数据）** + **时序分析（§4.5）** + 待补基线 | ⚠ **§4.4 缺口** |
| ⑤ 大模型协作记录 | → `llm-collaboration.md` | ✅ |
| ⑥ 技能包提炼过程 | → `../skill/README.md` | ✅ |
| ⑦ 复现说明 | → 根 `README.md` | ✅ |

---

## ⏳ 待办清单（**板到后按此执行**）

> 当前唯一影响评分的缺口：赛题 §3.3.5.5 把"性能与资源优化效果"
> 列为 **20 分**，且 §3.3.4 要求"给出**与基线的对比**"。
> 现有数据是**单点的，没有对比**。

### P1 · PS 侧基线（**最该做的**）

| 步骤 | 做法 |
|---|---|
| 1 | 把 [`../host/bench_ps_baseline.py`](../host/bench_ps_baseline.py) 拷到 PYNQ |
| 2 | 在板上跑：`python3 bench_ps_baseline.py --json ps.json` |
| 3 | 与 PL 侧延迟对比 → **加速比** |

**为什么最重要**：直接回答赛题的核心问题"**为什么用 FPGA**"。

**可对照的先例**：CANDAR 2020 在 **PYNQ-Z1** 上测得
"**相比同一块板的 PYNQ ARM CPU 加速 22×**"（见 `design.md` §1.4②）。
本作品测出的数可与 22× 对照。

> ⚠ **主机 CPU 的数不能直接写进报告**。该脚本在 PC 上跑出约 3.8 ms/帧，
> 但那是 Ryzen 9 8940HX，**不是 Cortex-A9**，差着代际。
> 脚本里已标注它是"主机参考值，非 PS 基线"。

### P2 · 上板实测输出（**赛题硬性要求**）

| 步骤 | 产出 |
|---|---|
| 按 [`board-bringup-guide.md`](../docs/board-bringup-guide.md) 走分级验证 | — |
| 截下 Jupyter 里出的图 | **实测输出 + 与参考结果的比对** |
| 测实际帧率 / DMA 吞吐 / 预处理单帧耗时 | 替换 `design.md` §3.2 的"按设计推算"栏 |

> 赛题 §3.3.3.2："作品须**在板上跑通并提供实测输出**"。
> `design.md` §3.2 的吞吐量现在是**推算值**（已标注"非实测"）。

### P3 · HLS 优化前后对比 ✅ **已完成（2026-09-18）**

结果见 [`../src_hls/optimization-report.md`](../src_hls/optimization-report.md)。
**结论是负结果，但数据完整**：

- 探针证明 DSP 的根因（腐蚀流水线的索引算术：71 → 15）
- 但**改不掉**：换编译期步长无效、手写展开反而更差（71 → 86）
- **顺带查出 WNS `+0.265` 不属于本设计**，是 AMD `v_tc` IP 的
- **顺带核对出 3 处凭记忆写错的资源数字**（LUT `13,184`→`13,144`、
  Fmax `137`→`143.31 MHz`），已更正
- 工具用上了：[`../skill/pynq/golden_compare.py`](../skill/pynq/golden_compare.py)，
  同时**发现并修掉了它的一个静默失败**（见 `skill/pynq/README.md`）

> ⚠ **DSP 的大头在 `morph_stage`（56 个），不在 `thresh_stage`（2 个）。**
> 项目里曾有四处写反。
>
> ⚠ **旧警告已作废**：这里原先写"唯一能降 DSP 的改法会让 II 从 1 退化成 2"。
> 经 2026-09-18 实测，**从未出现过 II=2**（全程 II=1），
> 而能降 DSP 的写法代价是**资源反而增加**（71→86）。详见 `optimization-report.md`。

### P4 · 上电前的硬件复核（**板到后第一件事**）

| 项 | 为什么 |
|---|---|
| **引脚万用表复核** | XDC 映射是**推理**的（原理图"镜像编号"），**插错会 3V3/GND 反接烧板**（步骤见 `board-bringup-guide.md` §2.2） |
| 短路检查 | 3V3 对 GND 的电阻 |
| **OV5640 出图验证** | 配置表已换 250 条真表，但**未上板验证过** |
