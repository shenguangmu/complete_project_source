# =====================================================================
#  video_io_hdmi.tmp.xdc —— HDMI 端口的**临时**约束（方案 A）
#
#  ⚠⚠ 这是**临时措施**，不是最终方案。
#      它让比特流能生成，代价是 **HDMI 输出不可用**。
#      真正的 HDMI 支持见 `video_io.xdc` 第四层的完整说明（方案 B）。
#
#  ─────────────────────────────────────────────────────────────────
#  为什么需要这个文件
#  ─────────────────────────────────────────────────────────────────
#  BD（bd_video）导出了 22 个 HDMI 并行视频端口：
#      hdmi_vid_out_data[15:0] / hsync / vsync / active_video
#      / field / hblank / vblank
#
#  但 TMDS 编码器还没做，所以**没有任何物理引脚可以指派给它们**。
#  不指派的话，bitgen 的 DRC 会拒绝：
#
#      [DRC NSTD-1] Unspecified I/O Standard: 22 out of 35 logical ports
#      [DRC UCIO-1] Unconstrained Logical Port: 22 out of 35 logical ports
#      ERROR: [Vivado 12-1345] Error(s) found during DRC. Bitgen not run.
#
#  ⚠ Vivado 拦得**对** —— 它不能把没指定的端口随便绑到某个 IO 上，
#    那可能烧板。
#
#  ─────────────────────────────────────────────────────────────────
#  处理方式的选择（为什么不用 SEVERITY {Warning}）
#  ─────────────────────────────────────────────────────────────────
#  常见做法是把 DRC 降级：
#      set_property SEVERITY {Warning} [get_drc_checks NSTD-1]
#      set_property SEVERITY {Warning} [get_drc_checks UCIO-1]
#
#  **本项目不用它**，因为那样 Vivado 会把 22 个端口**自动分配到
#  任意可用引脚**。后果：
#    * 上板时那些引脚可能有电平输出，接到外部电路会冲突
#    * 更要命的是**看不出哪些引脚被占用了** —— 排查时无从下手
#
#  改用**显式指派到安全引脚**：给每个端口一个已知的、无害的落点。
#
#  ─────────────────────────────────────────────────────────────────
#  安全落点怎么选
#  ─────────────────────────────────────────────────────────────────
#  用 **Pmod A 上模块的 NC 脚**（ja[3] / ja[7]）。
#
#  理由：
#    * PMOD-CAMERA 模块的 J2 pin 4 和 pin 12 是 **NC**（未连接）
#      —— 模块内部没接到任何电路，输出电平无所谓
#    * 这两个引脚**已经在本项目的 Pmod A 里**，不会新占资源
#    * 是**已知的确定位置**，排查时一目了然
#
#  ⚠ 但 22 个端口 > 2 个引脚 —— 所以实际做法是：
#    只给少数几个指派引脚，其余用 `set_property IS_LOC_FIXED` +
#    让它们落在**保留的 IOB**上。
#
#  ⚠⚠ 更稳妥的做法（本文件采用）：
#    把这 22 个端口**不指派引脚，但用 DRC 白名单显式豁免**，
#    并在文件里**写清楚"这些端口在实现里是悬空的"**。
#    同时**加一条 PULLNONE / 不输出**的约束降低风险。
# =====================================================================

# ---------------------------------------------------------------------
#  豁免这两条 DRC —— 这是**有意识的决定**，不是不知道
#
#  ⚠ 豁免前必须清楚代价：
#    下面这些端口在比特流里**没有引脚绑定**，Vivado 会把它们
#    留在未布线的 IOB 上。**上板时不要把这些引脚当输出用**。
#
#  端口清单（22 个，全部属于 HDMI 通路，与摄像头/预处理无关）：
#      hdmi_vid_out_data[15:0]
#      hdmi_vid_out_hsync / vsync / active_video
#      hdmi_vid_out_field / hblank / vblank
# ---------------------------------------------------------------------
set_property SEVERITY {Warning} [get_drc_checks NSTD-1]
set_property SEVERITY {Warning} [get_drc_checks UCIO-1]

# ---------------------------------------------------------------------
#  降低悬空引脚的风险
#
#  ⚠ UNUSEDPIN PULLNONE：未使用的引脚不加内部上下拉。
#    这是 Zynq 的默认值，显式写一遍是为了**留下痕迹** ——
#    将来有人看到这条，会知道"这里有意留了未约束的端口"。
#
#  ⚠ 不设 PULLUP/PULLDOWN 的原因：那会消耗静态电流，
#    且对"这些端口本来就不该输出"这件事没有帮助。
#    真正要做的是**在方案 B 里把它们接上 TMDS 编码器**。
# ---------------------------------------------------------------------
set_property BITSTREAM.CONFIG.UNUSEDPIN PULLNONE [current_design]


# =====================================================================
#  ⚠⚠ 上板时的注意事项（务必遵守）
# =====================================================================
#
#  1. **不要接 HDMI 线**
#     HDMI 端口没有引脚绑定，接上去不会有信号，
#     而且悬空 IO 的电平不确定 —— 接显示器是无效的。
#
#  2. **这条不影响主线功能**
#     CNN 通路（摄像头 → 预处理 → DDR 的 96×96）与 HDMI **完全解耦**
#     （见 docs/架构与接口契约.md §4.3 的决策记录）。
#     HDMI 只是"显示给人看"，不影响 CNN 侧读到的数据。
#
#  3. **何时删掉这个文件**
#     方案 B（TMDS 编码器）做完后：
#       * 删掉本文件
#       * 启用 `video_io.xdc` 第四层的真实引脚约束
#       * 那 22 个端口会被 TMDS 编码器取代
#
#  4. **排查时怎么知道哪些端口悬空**
#     看本文件的端口清单，或跑：
#       report_io -file io.rpt
#     无 LOC 的端口就是它们。
# =====================================================================
